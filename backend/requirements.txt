# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Database
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.2

# Data validation
pydantic==2.9.2
pydantic-settings==2.5.2

# Document processing
PyMuPDF==1.24.10
python-docx==1.1.2

# Machine Learning / NLP
scikit-learn==1.5.2
pandas==2.2.2
numpy==1.26.4
# Embeddings run on ONNX Runtime (via fastembed) instead of PyTorch. This
# drops ~200 MB of import cost and ~380 MB of inference overhead, which is
# what makes the API fit a 512 MB instance. Same all-MiniLM-L6-v2 weights.
fastembed==0.8.0
onnxruntime==1.30.0

# Utilities
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
httpx==0.27.2

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
